import 'package:device_frame/device_frame.dart';
import 'package:flutter/widgets.dart';

class DeviceModels {
  DeviceInfo device;
  IconData data;

  DeviceModels({required this.device, required this.data});
}
